# Optional: Jupyter analysis stack (install on top of requirements.txt)
jupyter>=1.0.0
nbformat>=5.9.0
seaborn>=0.13.0
# Required for Cursor/VS Code / Jupyter to attach to this venv as a notebook kernel
ipykernel>=6.29.0
